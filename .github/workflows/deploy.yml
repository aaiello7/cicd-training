name: Deploy Andres

on:
  push:
    branches:
      - master

jobs:
    deploy:
        runs-on: ubuntu-latest
    
        steps:
        - name: Checkout
            uses: actions/checkout@v2
    
        - name: Test flow
            run: echo "Test flow"
