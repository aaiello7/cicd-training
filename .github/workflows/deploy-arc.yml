name: Deploy Arc Runners

on:
    pull-request:
        branches:
        - master

jobs: 
    deploy-arc:
        runs-on: ["innersource.prod.amr.dind"]
        steps:
          - name: hello-world
            run: echo 'Hello, World!'